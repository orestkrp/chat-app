{
  "name": "messenger",
  "version": "1.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "packageManager": "npm@10.8.1",
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
